# Render Deployment Environment Variables
# These will be automatically set by render.yaml
# Or manually add them in Render Dashboard

# API Configuration
SECRET_KEY=wf-gMr8kevo-Dyumh0b2p8VD5q0xt_rXSxfLbvL-XwU
ALLOWED_ORIGINS=*
WORKERS=4
LOG_LEVEL=info

# Security
RATE_LIMIT_PER_MINUTE=100
MAX_UPLOAD_SIZE_MB=50
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Feature Flags
ENABLE_EXPLAINABILITY=true
ENABLE_ACTIVE_LEARNING=false
ENABLE_DRIFT_DETECTION=false

# Database (if using Render PostgreSQL)
# DATABASE_URL will be automatically set by Render

# Redis (if using Render Redis)
# REDIS_URL will be automatically set by Render

# Model Paths (optional)
PHISHING_MODEL_PATH=./models/checkpoints/phishing_vit_best.ckpt
MALWARE_MODEL_PATH=./models/checkpoints/malware_ensemble_best.ckpt

# NOTE: Update ALLOWED_ORIGINS with your actual frontend domain in production
# Example: ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com
